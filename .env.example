# CORS
BACKEND_ALLOWED_ORIGINS=*

# Cache TTL
BACKEND_CACHE_TTL_EST=3
BACKEND_CACHE_TTL_NAV=3600

# Prewarm codes (comma separated)
BACKEND_WATCH_CODES=110022,161725

# Optional data source tokens/cookies
# tushare（可选）：Tushare Pro token
TUSHARE_TOKEN=

# xueqiu（可选）：雪球 Cookie（可能会过期，需要更新）
XUEQIU_COOKIE=

# 下面两个目前是占位（后端未接入），无需申请
JOINQUANT_TOKEN=
RICEQUANT_TOKEN=

# akshare（可选）：不需要 token，但需要安装 akshare 并启用
AKSHARE_ENABLED=0
